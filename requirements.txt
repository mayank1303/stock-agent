# Core data + agent
yfinance>=0.2.40
requests>=2.31.0
pandas>=2.0.0
anthropic>=0.40.0
python-dotenv>=1.0.0

# MCP server (Claude Desktop integration)
mcp>=1.0.0

# Web backend
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0

# Local LLM (optional - Ollama must be installed separately)
ollama>=0.3.0

# RAG: embeddings + vector store
sentence-transformers>=3.0.0
chromadb>=0.5.0

# RAG: book ingestion (PDF/EPUB + OCR)
# OCR also needs system packages: brew install tesseract poppler
pypdf>=4.0.0
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0.0
ebooklib>=0.18
beautifulsoup4>=4.12.0