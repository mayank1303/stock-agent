Drop your trading book PDFs in this folder, then run:

    python3 -m rag.ingest_books

PDFs are gitignored (personal/copyrighted content) - this folder is
tracked so the expected location exists after cloning the repo, but
the actual books never get committed.